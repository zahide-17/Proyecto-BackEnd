// Categoria.js
/** Clase que representa la categoria a la que pertenece un producto*/
class Categoria {

    constructor(idCategoria,nombre) {
        this.idCategoria = idCategoria; //Identificador de la categoria
        this.nombre = nombre; // Nombre de la categoria
    }
}

module.exports = Categoria;